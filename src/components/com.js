import React from "react";

function Com(){
    return(
        <div>
            <h1>Hello {this.props.name} Also Known As {this.props.title}
            </h1>
        </div>
    )
}
export default Com